
public class SemanticAnalysis{

  enum Type{
    INT,
    INT_ARR,
    BOOLEAN,
    VOID;
  }

 // public Object visit(SimpleNode node, SymbolTable data) {
    // String name = (String)node.jjtGetValue();

    // consultar symboltable, ver se nome existe

    // se sim, retorna TYPE do node
    // se não, retorna TYPE.Erro

    //Type identifierType = node.jjtGetChild(0).visit(this, data);

    //if (identifierType == ERRO) {
      // retorna TYPE.Erro
    //}
  // }
}
